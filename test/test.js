'use strict';

var expect = require('chai').expect;
var numFormatter = require('../index');

describe('#indianGirl', function() {
	it('Contained Girl Name' , function() {
		var x;
		if(x > 0) {
			return true;
		} else {
			return false;
		}
	});
});